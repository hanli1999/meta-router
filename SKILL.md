---
name: meta-router
description: 总入口路由器 — 把 Claude 当前会话能调用的所有能力（skill / 工具 / 工作流 / 子agent / memory）做成一张路由表，用户问"做 X 该用什么"立即给出最完整路径。触发词："做 X 该用什么"、"X 用什么 skill"、"总入口"、"meta-router"、"怎么 X"。
---

# 总入口路由器（meta-router）

> 不做事，只路由：你说"想 X"，它告诉你调哪个 skill / 用哪个工具 / 走哪个组合。

## 1. 标题卡

- **name**: `meta-router`
- **description**: 总入口路由器 — 把 Claude 当前会话能调用的所有能力做成路由表
- **版本**: v1.0.0
- **创建日期**: 2026-09-21

## 2. 原则 / 规矩（红线 5 条）

### 2.1 不做事，只路由
- 本 skill 的唯一职责是**告诉用户/Claude 该调什么**
- 不写代码、不查资料、不做分析——这些是其他 skill / 工具的活

### 2.2 路由基于真实工具
- 路由表里的每条路径必须有**实际可调用的 skill / 工具**
- 不虚报能力（"无所不能"是营销词，"能做 A/B/C/D"是事实）

### 2.3 优先级明确
- 用户明确说的 skill > 用户明确说的工具 > 触发词推断 > 类比启发
- 冲突时按规则表仲裁

### 2.4 不打折扣
- 推荐路径要给**完整链路**（不只是单个 skill，可能要 skill + 工具 + memory 组合）
- 例："做巴菲特灵宝" ≠ "用 lingbao-analyzer"，而是 forge + WebSearch + Write + gh CLI 四步

### 2.5 边界诚实
- 路由表**不隐瞒做不到的事**（物理操作、24/7 持续、100% 准确）
- 边界在 §8 明说

## 3. 开工前准备（前置条件）

### 3.1 路由资产（已固化）

| 资产 | 路径 | 规模 |
|---|---|---|
| 能力矩阵 | `references/capability-matrix.md` | 6 大能力 × 子能力 × 限制 |
| 路由规则 | `references/routing-rules.md` | 20 条规则 |
| 典型用例 | `references/use-cases.md` | 8 个真实用例 |

### 3.2 触发时输入

| 字段 | 必填 | 说明 |
|---|---|---|
| 用户原话 | ✅ | 用户问句的完整文本 |
| 推荐深度 | ❌ | 极简（只给 skill 名）/ 标准（给命令）/ 完整（给完整链路） |

### 3.3 已注册子 skill

| Skill 名 | 用途 | 路由触发词 |
|---|---|---|
| `xiuxian` | 凡人修仙传类比 | "按凡人修仙传看 X" |
| `lingbao-analyzer` | 灵宝类比生成 | "灵宝化 X"、"做灵宝 X" |

## 4. 步骤（工作流程）

### Step 1：判定用户原话类型
- 明确指向某 skill（如 `/xiuxian`）→ 直接路由
- 明确指向某工具（如 "用 WebSearch"）→ 直接路由
- 一般问句 → 进 Step 2

### Step 2：查路由规则表
- 在 `references/routing-rules.md` 的 20 条规则里找匹配
- 命中 → 推荐路径

### Step 3：未命中时的兜底
- 用 xiuxian skill 类比启发（灵宝元素可参考）
- 或问用户澄清（AskUserQuestion）

### Step 4：输出推荐链路
- **极简**：`skill 名` 或 `工具名`
- **标准**：`skill 名 + 真实命令`
- **完整**：`多步链路（skill + 工具 + memory）`

### Step 5：标注预期输出
- 让用户知道调用后会得到什么

## 5. 工具

### 5.1 核心脚本

| 脚本 | 作用 |
|---|---|
| `scripts/route.py` | 给定 query → 返回最匹配的 skill/工具 + 推荐理由 |

### 5.2 用法示例

```bash
# 标准查询
python3 scripts/route.py --query "把芒格做成灵宝"

# 极简（只输出 skill 名）
python3 scripts/route.py --query "X" --format short

# JSON 输出（机器可读）
python3 scripts/route.py --query "X" --format json
```

### 5.3 调用外部 skill/工具

路由结果本身就是**调用指令**，实际执行靠：
- `Bash(command="python3 .../skill.py", ...)`
- `WebSearch(query="...")` / `WebFetch(url="...", prompt="...")`
- `Agent(prompt="...")`
- `CronCreate(...)` / `Write(...)` 等

## 6. 工具坏了（降级策略）

| 故障 | 应对 |
|---|---|
| 路由规则表缺失 | 重新创建 `references/routing-rules.md`（20 条已固化） |
| 路由脚本报错 | 退到 Step 2 人工查表 |
| 子 skill 不可用 | 明说"X skill 当前不可用，请先检查安装" |
| 用户问"AI 做不到的" | 按 §8 边界声明处理 |

## 7. 检查清单

### 7.1 路由质量
- [ ] 推荐路径必须有可执行命令（不是抽象建议）
- [ ] 子能力 ≥ 3 个时给出组合方案
- [ ] 边界诚实（不虚报能力）
- [ ] 优先级清晰（skill > 工具 > 推断）

### 7.2 输出格式
- [ ] Markdown 含推荐路径 + 真实命令 + 预期输出
- [ ] 不含免责声明/道德说教
- [ ] 路径深度按用户要求（极简/标准/完整）

### 7.3 跨会话一致性
- [ ] 同一 query 多次调用结果一致
- [ ] memory 里的偏好影响路由（用户偏好记入后优先用）

## 8. 不要做什么

### 8.1 不做的事
- ❌ 不写代码（让 Claude 本职或对应 skill 做）
- ❌ 不查资料（让 WebSearch/WebFetch 做）
- ❌ 不做数据分析（让 Bash + Python 做）
- ❌ 不假装"无所不能"（按真实工具清单）

### 8.2 不推荐的路径
- ❌ 不推荐未安装的 skill（必须先确认 skill 存在）
- ❌ 不推荐需要付费 API 而用户没授权的工具
- ❌ 不推荐破坏性操作（`rm -rf`、`git push --force`）除非用户明确说

### 8.3 真实边界（诚实声明）

| 边界 | 原因 |
|---|---|
| 物理世界操作 | 无机器人接口 |
| 24/7 持续在线 | 单次会话有上下文限制 |
| 100% 准确 | 幻觉存在，重要决策双重核对 |
| 法律责任 | 签字/合同主体是你 |
| 医疗/法律最终决策权 | 建议可以，决定是你的 |

## 9. 小贴士（踩坑沉淀）

### 9.1 路由不是机械匹配
- 用户说"我想做点东西"，可能命中规则 #1-#20 中任何一条
- 关键看**关键词密度**和**意图分类**

### 9.2 组合调用比单调用更常见
- 80% 的真实需求需要 ≥ 2 步（如"做 X 灵宝 + 开源" = forge + Write + gh CLI）
- 路由时主动给出完整链路，不要让用户自己拼

### 9.3 类比启发是兜底
- 用户问得太抽象时（如"如何提升自己"），用 xiuxian 找类比
- 不要硬塞工具

### 9.4 用户偏好优先
- 如果 memory 里记了用户偏好（如"凡是用中文"），路由结果必须尊重
- 路由前查 memory

### 9.5 不要过度推荐
- 简单问题不要复杂化（"查个天气"不需要 Workflow）
- 路由复杂度 = 任务复杂度

## 10. 下一步（触发方式）

### 10.1 触发词

| 触发词 | 行为 |
|---|---|
| "做 X 该用什么" | 路由到推荐 skill/工具 |
| "X 用什么 skill" | 同上 |
| "总入口" / "meta-router" | 加载本 skill |
| "怎么 X" / "如何 X" | 同上 |

### 10.2 后续动作
1. 用户问"做 X" → 触发 skill → 查路由规则表
2. 输出推荐路径 → 用户可追问
3. 追问（如"具体怎么调"）→ 输出真实命令
4. **失败兜底**：所有规则不命中 → 用 xiuxian 类比启发

### 10.3 跨 skill 联动（2026-09-21 用户核心洞察）

> "xiuxian 解决'想什么'，AI 工具解决'怎么做'，持久化解决'不忘事'，并行解决'加快'——四者叠加接近无所不能"

**联动协议**：
- `xiuxian`（类比）→ 给"修仙视角"
- `WebSearch`/`WebFetch`（检索）→ 给"真实数据"
- `lingbao-analyzer`（灵宝化）→ 给"完整方案"
- `Bash`/`Write`/`Edit`（执行）→ 给"落地产物"
- `memory`（持久化）→ 给"跨会话记忆"
- `Cron`（定时）→ 给"自动运行"
- `Workflow`（并行）→ 给"规模化"

## 11. 案件管家联动

### 11.1 联动策略

| 情形 | 是否联动 | 处理 |
|---|---|---|
| 用户当前有激活案件 | ✅ 必须联动 | 报告末尾追加"案件回写提示" |
| 用户无激活案件 | ❌ 不联动 | 仅一行"当前未激活案件，无需登记" |
| 路由结果用于决策（投资/招聘）| ✅ 联动 | 引用时登记来源 |

### 11.2 联动协议
- **协议来源**：`skills/case-manager/references/downstream-writeback-protocol.md`
- **入口插槽**：`/case-manager` SKILL.md §0.7
- **本 skill 的产出**：Markdown 路由报告
- **case-manager 接收字段**：用户原话 / 推荐路径 / 真实命令 / 预期输出

### 11.3 标准挂载块

```markdown
---
## 案件管家联动
- 当前案件：{有/无}
- 是否登记到台账：{待用户确认/否}
- 如登记，需提供字段：用户原话 / 推荐路径 / 预期输出
```

---

**How to apply**：用户问"做 X 该用什么" → 立即调用 `python3 scripts/route.py --query "X"` → 把推荐路径 + 真实命令 + 预期输出呈现给用户。
